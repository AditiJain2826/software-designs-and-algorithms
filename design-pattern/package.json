{
    "name": "design-patterns",
    "version": "1.0.0",
    "main": "build/index.js",
    "scripts": {
        "build": "rimraf ./build && tsc",
        "dev": "nodemon",
        "start": "npm run build && node dist/index.js"
    },
    "devDependencies": {
        "nodemon": "^2.0.15",
        "rimraf": "^3.0.0",
        "ts-node": "^10.4.0",
        "typescript": "^4.6.2"
    },
    "dependencies": {
        "@types/node": "^20.9.0"
    }
}
